name: surg-curve
root: /home/thadryan/Vaults/Projects/Work/Primary/SurgicalRobots/LearningCurve
windows:
  - code:
      layout: main-vertical
      panes:
        - bash -c "nvim; exec bash"
        - bash
  - git:
      panes:
        - bash -c "lazygit; exec bash"
  - bash:
      panes:
        - bash
