name: oud-predictors-ml
root: /home/thadryan/Vaults/Projects/Work/Secondary/IndividualOUD
windows:
  - code:
      panes:
        - bash -c "nvim; exec bash"
        - bash
      commands:
        - tmux resize-pane -t 0 -x 66%
  - git:
      panes:
        - bash -c "lazygit; exec bash"
  - bash:
      panes:
        - bash
