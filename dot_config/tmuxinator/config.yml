name: config
root: ~/Vaults/Projects/Dotfiles
windows:
  - code:
      layout: main-vertical
      panes:
        - bash -c "nvim; exec bash"
        - bash
  - git:
      panes:
        - bash -c "lazygit; exec bash"
  - bash:
      panes:
        - bash
