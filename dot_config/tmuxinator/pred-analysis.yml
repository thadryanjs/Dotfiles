name: pred-analysis
root: /home/thadryan/Vaults/Projects/Home/PredAnalysis
windows:
  - local:
      layout: main-vertical
      panes:
        - bash -c "nvim; exec bash"
        - bash
      commands:
        - tmux resize-pane -t 0 -x 80%
  - git:
      panes:
        - bash -c "lazygit; exec bash"
  - bash:
      layout: main-vertical
      panes:
        - bash
        - bash
