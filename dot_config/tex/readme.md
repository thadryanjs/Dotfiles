
If I get r-tinytex working on arch, I can just add things to the file in this dir and run:

``` 
tlmgr install $(cat packages.txt)
```

...when I need new stuff.

The terminal, a box full of text,
Where commands put the power to test.
With pipes, grep, and stdin,
It does what you bid,
And never asks why or what's best.
